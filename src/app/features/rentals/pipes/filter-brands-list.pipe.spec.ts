import { FilterBrandsListPipe } from './filter-brands-list.pipe';

describe('FilterBrandsListPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBrandsListPipe();
    expect(pipe).toBeTruthy();
  });
});
